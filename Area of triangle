#include <stdio.h>
int main()
{
	float b,h,area;
	printf("Enter the base and height of the triangle \n");
	scanf("%f%f", &b, &h);
	area= (b * h)/2;
	printf("The area of triangle is = %.2f \n",area);
	return 0;
}
